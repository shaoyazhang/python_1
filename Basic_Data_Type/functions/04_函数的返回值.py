'''
    返回值：函数执行只会，会给调用方一个结果，这个结果就是返回值
    
    关于return：
    1. 如果函数内没有return，此时外界收到的是None
    2. 如果写了return:
        a. 只写了return，后面没有数据，此时接收到的依然是None
        b. return 值，此时函数有一个返回值，外界能收到一个数据
        c. return 值1，值2，值3，.... 外界能收到元组，存放所有返回值
'''

